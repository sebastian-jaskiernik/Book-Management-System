@model WebApplication3.Models.Publisher

<tr>
    <td>@Model.Name</td>
    <td>
        <a asp-action="Update" asp-route-id="@Model.PublisherId" class="btn btn-warning">Edit</a>
        <a asp-action="Delete" asp-route-id="@Model.PublisherId" class="btn btn-danger">Delete</a>
    </td>
</tr>